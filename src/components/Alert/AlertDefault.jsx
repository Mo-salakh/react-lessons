

const AlertDefault = (props) => {

    return(
        <div>
            <p>{props.nameAlert}</p>
            <p>{props.ageAlert}</p>
            <p>{props.cityAlert}</p>
            <p>{props.familyAlert}</p>
        </div>
    )

}

export default AlertDefault;