import AlertDefault from "../Alert/AlertDefault";

const Text = ({name, age , city , family}) =>{
    return (<AlertDefault nameAlert = {name} ageAlert = {age} cityAlert={city} familyAlert={family} />) 
}

export default Text;